# Errors

Use this folder for hard debugging notes.

Each error note should include:

- symptom
- command or user action that reproduced it
- root cause once known
- fix or workaround
- regression test or validation command

Do not record transient noise unless it taught a reusable debugging lesson.
