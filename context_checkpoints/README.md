# Context Checkpoints

Use this folder for end-of-session handoff snapshots.

Each checkpoint should include:

- current state
- completed work
- in-progress or unresolved items
- changed source subsystems
- validation evidence
- next concrete step

Checkpoints are more detailed than `context.md` but less broad than logs.
