# Context History

Archive prior versions of `context.md` here before rewriting it when the live
state meaningfully changes.

Naming suggestion:

```text
YYYYMMDD_HHMM_context_before_[short-topic].md
```

Do not use this folder as a dumping ground for all notes. It exists so the live
`context.md` can stay concise without losing prior restart snapshots.
