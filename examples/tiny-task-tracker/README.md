# Tiny Task Tracker Example

This is a filled example of Context Architecture Bundle for a small task
tracking app. It is intentionally simple so users can compare the blank template
with a populated version.

The imagined app has:

- a Python FastAPI backend
- a SQLite database
- a browser UI
- pytest validation

Start with `context.md`, then inspect `architecture/code-map.md`.
